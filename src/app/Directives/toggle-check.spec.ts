import { ToggleCheck } from './toggle-check';

describe('ToggleCheck', () => {
  it('should create an instance', () => {
    const directive = new ToggleCheck();
    expect(directive).toBeTruthy();
  });
});
